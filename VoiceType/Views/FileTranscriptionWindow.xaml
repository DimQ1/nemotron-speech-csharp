<Window x:Class="VoiceType.Views.FileTranscriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:VoiceType.ViewModels"
        Title="Transcribe Audio File"
        Width="720" Height="560"
        MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource BgBrush}">
    <Window.DataContext>
        <vm:FileTranscriptionViewModel/>
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Audio File" Style="{StaticResource DarkGroupBox}">
            <StackPanel>
                <TextBlock Text="Select a WAV or MP3 file to transcribe using the current VoiceType engine settings."
                           Foreground="{StaticResource FgSecondaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding SelectedFile, Mode=OneWay}"
                             Style="{StaticResource DarkTextBox}"
                             IsReadOnly="True"
                             VerticalContentAlignment="Center"/>
                    <Button Grid.Column="1"
                            Content="Browse..."
                            Margin="8,0,0,0"
                            Background="{StaticResource BgTertiaryBrush}"
                            Foreground="{StaticResource FgBrush}"
                            BorderThickness="0"
                            Padding="12,6"
                            Cursor="Hand"
                            Command="{Binding BrowseFileCommand}"/>
                    <Button Grid.Column="2"
                            Content="{Binding ActionButtonText}"
                            Margin="8,0,0,0"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding TranscribeCommand}"/>
                </Grid>
            </StackPanel>
        </GroupBox>

        <Border Grid.Row="1"
                Margin="0,12,0,12"
                Padding="12,10"
                CornerRadius="6"
                Background="{StaticResource BgSecondaryBrush}">
            <StackPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource FgBrush}" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding EngineSummary}" Foreground="{StaticResource FgSecondaryBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="2" Header="Recognized Text" Style="{StaticResource DarkGroupBox}">
            <TextBox Text="{Binding ResultText, Mode=OneWay}"
                     Style="{StaticResource DarkTextBox}"
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>
        </GroupBox>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="The result is post-processed with the same rules used by live dictation."
                       Foreground="{StaticResource FgSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
            <Button Grid.Column="1"
                    Content="📋 Copy"
                    Style="{StaticResource PrimaryButton}"
                    Padding="14,8"
                    Margin="12,0,0,0"
                    Command="{Binding CopyCommand}"/>
        </Grid>
    </Grid>
</Window>
